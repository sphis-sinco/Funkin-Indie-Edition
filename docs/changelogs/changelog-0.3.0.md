# 0.3.0 (1/20/2026)

- Changed: `MiscScript` and `CharacterScript` is and are now just `GeneralScript` since there is no actual real difference between the,
- 💖 Fixed: When `CoolUtil.coolTextFile` can't find the text file it doesn't cause a crash anymore
- 💖 Added: `danceOnCreation` and `flipHorizontalSingingAsPlayer` properties to character JSONS
- Changed: Softcoded some more character stuff
- 💖 Changed: Character offset files are loaded after loading character JSONS
- 💖 Changed: `miscScriptsLoaded` is now `scriptsLoaded` for scripts
- Added: Script creation traces
- 💖 Added: CHARACTER SCRIPTS!
